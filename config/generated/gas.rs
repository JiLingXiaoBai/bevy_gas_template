/*!
<auto-generated>
    This code was generated by a tool.
    Changes to this file may cause incorrect behavior and will be lost if
    the code is regenerated.
</auto-generated>
*/

use super::{ByteBuf, Decode, DecodeError, LubanError};
use std::sync::Arc;

/// Defines the supported ActionKind configuration values.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[repr(i32)]
pub enum ActionKind {
    /// Selects ApplyEffect.
    ApplyEffect = 0,
    /// Selects EndAbility.
    EndAbility = 1,
}

impl TryFrom<i32> for ActionKind {
    type Error = DecodeError;

    fn try_from(value: i32) -> Result<Self, Self::Error> {
        match value {
            0 => Ok(Self::ApplyEffect),
            1 => Ok(Self::EndAbility),
            _ => Err(DecodeError::new(
                0,
                format!("invalid ActionKind value: {value}"),
            )),
        }
    }
}

impl Decode for ActionKind {
    fn decode(buf: &mut ByteBuf) -> Result<Self, DecodeError> {
        let offset = buf.offset();
        let value = i32::decode(buf)?;
        Self::try_from(value)
            .map_err(|_| DecodeError::new(offset, format!("invalid ActionKind value: {value}")))
    }
}
/// Defines the supported AttributeRegion configuration values.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[repr(i32)]
pub enum AttributeRegion {
    /// Selects Hot.
    Hot = 0,
    /// Selects Cold.
    Cold = 1,
}

impl TryFrom<i32> for AttributeRegion {
    type Error = DecodeError;

    fn try_from(value: i32) -> Result<Self, Self::Error> {
        match value {
            0 => Ok(Self::Hot),
            1 => Ok(Self::Cold),
            _ => Err(DecodeError::new(
                0,
                format!("invalid AttributeRegion value: {value}"),
            )),
        }
    }
}

impl Decode for AttributeRegion {
    fn decode(buf: &mut ByteBuf) -> Result<Self, DecodeError> {
        let offset = buf.offset();
        let value = i32::decode(buf)?;
        Self::try_from(value).map_err(|_| {
            DecodeError::new(offset, format!("invalid AttributeRegion value: {value}"))
        })
    }
}
/// Defines the supported DurationKind configuration values.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[repr(i32)]
pub enum DurationKind {
    /// Selects Instant.
    Instant = 0,
    /// Selects DurationTicks.
    DurationTicks = 1,
    /// Selects Infinite.
    Infinite = 2,
}

impl TryFrom<i32> for DurationKind {
    type Error = DecodeError;

    fn try_from(value: i32) -> Result<Self, Self::Error> {
        match value {
            0 => Ok(Self::Instant),
            1 => Ok(Self::DurationTicks),
            2 => Ok(Self::Infinite),
            _ => Err(DecodeError::new(
                0,
                format!("invalid DurationKind value: {value}"),
            )),
        }
    }
}

impl Decode for DurationKind {
    fn decode(buf: &mut ByteBuf) -> Result<Self, DecodeError> {
        let offset = buf.offset();
        let value = i32::decode(buf)?;
        Self::try_from(value)
            .map_err(|_| DecodeError::new(offset, format!("invalid DurationKind value: {value}")))
    }
}
/// Defines the supported MagnitudeKind configuration values.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[repr(i32)]
pub enum MagnitudeKind {
    /// Selects Flat.
    Flat = 0,
    /// Selects LinearLevel.
    LinearLevel = 1,
}

impl TryFrom<i32> for MagnitudeKind {
    type Error = DecodeError;

    fn try_from(value: i32) -> Result<Self, Self::Error> {
        match value {
            0 => Ok(Self::Flat),
            1 => Ok(Self::LinearLevel),
            _ => Err(DecodeError::new(
                0,
                format!("invalid MagnitudeKind value: {value}"),
            )),
        }
    }
}

impl Decode for MagnitudeKind {
    fn decode(buf: &mut ByteBuf) -> Result<Self, DecodeError> {
        let offset = buf.offset();
        let value = i32::decode(buf)?;
        Self::try_from(value)
            .map_err(|_| DecodeError::new(offset, format!("invalid MagnitudeKind value: {value}")))
    }
}
/// Defines the supported ModifierOperation configuration values.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[repr(i32)]
pub enum ModifierOperation {
    /// Selects Add.
    Add = 0,
    /// Selects PercentAdd.
    PercentAdd = 1,
    /// Selects Multiply.
    Multiply = 2,
    /// Selects Override.
    Override = 3,
}

impl TryFrom<i32> for ModifierOperation {
    type Error = DecodeError;

    fn try_from(value: i32) -> Result<Self, Self::Error> {
        match value {
            0 => Ok(Self::Add),
            1 => Ok(Self::PercentAdd),
            2 => Ok(Self::Multiply),
            3 => Ok(Self::Override),
            _ => Err(DecodeError::new(
                0,
                format!("invalid ModifierOperation value: {value}"),
            )),
        }
    }
}

impl Decode for ModifierOperation {
    fn decode(buf: &mut ByteBuf) -> Result<Self, DecodeError> {
        let offset = buf.offset();
        let value = i32::decode(buf)?;
        Self::try_from(value).map_err(|_| {
            DecodeError::new(offset, format!("invalid ModifierOperation value: {value}"))
        })
    }
}
/// Defines the supported SelectionKind configuration values.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[repr(i32)]
pub enum SelectionKind {
    /// Selects SelfTarget.
    SelfTarget = 0,
    /// Selects ExplicitEntity.
    ExplicitEntity = 1,
    /// Selects Sphere.
    Sphere = 2,
    /// Selects Cone.
    Cone = 3,
}

impl TryFrom<i32> for SelectionKind {
    type Error = DecodeError;

    fn try_from(value: i32) -> Result<Self, Self::Error> {
        match value {
            0 => Ok(Self::SelfTarget),
            1 => Ok(Self::ExplicitEntity),
            2 => Ok(Self::Sphere),
            3 => Ok(Self::Cone),
            _ => Err(DecodeError::new(
                0,
                format!("invalid SelectionKind value: {value}"),
            )),
        }
    }
}

impl Decode for SelectionKind {
    fn decode(buf: &mut ByteBuf) -> Result<Self, DecodeError> {
        let offset = buf.offset();
        let value = i32::decode(buf)?;
        Self::try_from(value)
            .map_err(|_| DecodeError::new(offset, format!("invalid SelectionKind value: {value}")))
    }
}
/// Defines the supported SortOrder configuration values.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[repr(i32)]
pub enum SortOrder {
    /// Selects None.
    None = 0,
    /// Selects Nearest.
    Nearest = 1,
    /// Selects Farthest.
    Farthest = 2,
}

impl TryFrom<i32> for SortOrder {
    type Error = DecodeError;

    fn try_from(value: i32) -> Result<Self, Self::Error> {
        match value {
            0 => Ok(Self::None),
            1 => Ok(Self::Nearest),
            2 => Ok(Self::Farthest),
            _ => Err(DecodeError::new(
                0,
                format!("invalid SortOrder value: {value}"),
            )),
        }
    }
}

impl Decode for SortOrder {
    fn decode(buf: &mut ByteBuf) -> Result<Self, DecodeError> {
        let offset = buf.offset();
        let value = i32::decode(buf)?;
        Self::try_from(value)
            .map_err(|_| DecodeError::new(offset, format!("invalid SortOrder value: {value}")))
    }
}
/// Defines the supported TargetScope configuration values.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[repr(i32)]
pub enum TargetScope {
    /// Selects None.
    None = 0,
    /// Selects Primary.
    Primary = 1,
    /// Selects AllCaptured.
    AllCaptured = 2,
}

impl TryFrom<i32> for TargetScope {
    type Error = DecodeError;

    fn try_from(value: i32) -> Result<Self, Self::Error> {
        match value {
            0 => Ok(Self::None),
            1 => Ok(Self::Primary),
            2 => Ok(Self::AllCaptured),
            _ => Err(DecodeError::new(
                0,
                format!("invalid TargetScope value: {value}"),
            )),
        }
    }
}

impl Decode for TargetScope {
    fn decode(buf: &mut ByteBuf) -> Result<Self, DecodeError> {
        let offset = buf.offset();
        let value = i32::decode(buf)?;
        Self::try_from(value)
            .map_err(|_| DecodeError::new(offset, format!("invalid TargetScope value: {value}")))
    }
}

/// Stores one Ability configuration record.
#[allow(non_camel_case_types)]
#[derive(Debug, Clone)]
pub struct Ability {
    /// Configures id.
    pub id: i32,
    /// Configures name.
    pub name: String,
    /// Configures max_level.
    pub max_level: i32,
    /// Configures cost_effect_id.
    pub cost_effect_id: Option<i32>,
    /// Configures additional_cost_ids.
    pub additional_cost_ids: Vec<i32>,
    /// Configures cooldown_effect_id.
    pub cooldown_effect_id: Option<i32>,
    /// Configures asset_tags.
    pub asset_tags: Vec<String>,
    /// Configures required_tags.
    pub required_tags: Vec<String>,
    /// Configures blocked_tags.
    pub blocked_tags: Vec<String>,
    /// Configures cancel_ability_tags.
    pub cancel_ability_tags: Vec<String>,
    /// Configures block_ability_tags.
    pub block_ability_tags: Vec<String>,
    /// Configures targeting_id.
    pub targeting_id: i32,
    /// Configures allow_multiple_instances.
    pub allow_multiple_instances: bool,
    /// Configures task_ids.
    pub task_ids: Vec<i32>,
}

impl Decode for Ability {
    fn decode(buf: &mut ByteBuf) -> Result<Self, DecodeError> {
        let id: i32 = Decode::decode(buf).map_err(|error| error.context("Ability.id"))?;
        let name: String = Decode::decode(buf).map_err(|error| error.context("Ability.name"))?;
        let max_level: i32 =
            Decode::decode(buf).map_err(|error| error.context("Ability.max_level"))?;
        let cost_effect_id: Option<i32> =
            Decode::decode(buf).map_err(|error| error.context("Ability.cost_effect_id"))?;
        let additional_cost_ids: Vec<i32> =
            Decode::decode(buf).map_err(|error| error.context("Ability.additional_cost_ids"))?;
        let cooldown_effect_id: Option<i32> =
            Decode::decode(buf).map_err(|error| error.context("Ability.cooldown_effect_id"))?;
        let asset_tags: Vec<String> =
            Decode::decode(buf).map_err(|error| error.context("Ability.asset_tags"))?;
        let required_tags: Vec<String> =
            Decode::decode(buf).map_err(|error| error.context("Ability.required_tags"))?;
        let blocked_tags: Vec<String> =
            Decode::decode(buf).map_err(|error| error.context("Ability.blocked_tags"))?;
        let cancel_ability_tags: Vec<String> =
            Decode::decode(buf).map_err(|error| error.context("Ability.cancel_ability_tags"))?;
        let block_ability_tags: Vec<String> =
            Decode::decode(buf).map_err(|error| error.context("Ability.block_ability_tags"))?;
        let targeting_id: i32 =
            Decode::decode(buf).map_err(|error| error.context("Ability.targeting_id"))?;
        let allow_multiple_instances: bool = Decode::decode(buf)
            .map_err(|error| error.context("Ability.allow_multiple_instances"))?;
        let task_ids: Vec<i32> =
            Decode::decode(buf).map_err(|error| error.context("Ability.task_ids"))?;
        Ok(Self {
            id,
            name,
            max_level,
            cost_effect_id,
            additional_cost_ids,
            cooldown_effect_id,
            asset_tags,
            required_tags,
            blocked_tags,
            cancel_ability_tags,
            block_ability_tags,
            targeting_id,
            allow_multiple_instances,
            task_ids,
        })
    }
}
/// Stores one AbilityAdditionalCost configuration record.
#[allow(non_camel_case_types)]
#[derive(Debug, Clone)]
pub struct AbilityAdditionalCost {
    /// Configures id.
    pub id: i32,
    /// Configures resource.
    pub resource: String,
    /// Configures amount.
    pub amount: i64,
}

impl Decode for AbilityAdditionalCost {
    fn decode(buf: &mut ByteBuf) -> Result<Self, DecodeError> {
        let id: i32 =
            Decode::decode(buf).map_err(|error| error.context("AbilityAdditionalCost.id"))?;
        let resource: String =
            Decode::decode(buf).map_err(|error| error.context("AbilityAdditionalCost.resource"))?;
        let amount: i64 =
            Decode::decode(buf).map_err(|error| error.context("AbilityAdditionalCost.amount"))?;
        Ok(Self {
            id,
            resource,
            amount,
        })
    }
}
/// Stores one AbilityTask configuration record.
#[allow(non_camel_case_types)]
#[derive(Debug, Clone)]
pub struct AbilityTask {
    /// Configures id.
    pub id: i32,
    /// Configures at_tick.
    pub at_tick: i32,
    /// Configures kind.
    pub kind: crate::config::generated::gas::ActionKind,
    /// Configures target_scope.
    pub target_scope: crate::config::generated::gas::TargetScope,
    /// Configures effect_id.
    pub effect_id: Option<i32>,
}

impl Decode for AbilityTask {
    fn decode(buf: &mut ByteBuf) -> Result<Self, DecodeError> {
        let id: i32 = Decode::decode(buf).map_err(|error| error.context("AbilityTask.id"))?;
        let at_tick: i32 =
            Decode::decode(buf).map_err(|error| error.context("AbilityTask.at_tick"))?;
        let kind: crate::config::generated::gas::ActionKind =
            Decode::decode(buf).map_err(|error| error.context("AbilityTask.kind"))?;
        let target_scope: crate::config::generated::gas::TargetScope =
            Decode::decode(buf).map_err(|error| error.context("AbilityTask.target_scope"))?;
        let effect_id: Option<i32> =
            Decode::decode(buf).map_err(|error| error.context("AbilityTask.effect_id"))?;
        Ok(Self {
            id,
            at_tick,
            kind,
            target_scope,
            effect_id,
        })
    }
}
/// Stores one Attribute configuration record.
#[allow(non_camel_case_types)]
#[derive(Debug, Clone)]
pub struct Attribute {
    /// Configures name.
    pub name: String,
    /// Configures region.
    pub region: crate::config::generated::gas::AttributeRegion,
    /// Configures description.
    pub description: String,
}

impl Decode for Attribute {
    fn decode(buf: &mut ByteBuf) -> Result<Self, DecodeError> {
        let name: String = Decode::decode(buf).map_err(|error| error.context("Attribute.name"))?;
        let region: crate::config::generated::gas::AttributeRegion =
            Decode::decode(buf).map_err(|error| error.context("Attribute.region"))?;
        let description: String =
            Decode::decode(buf).map_err(|error| error.context("Attribute.description"))?;
        Ok(Self {
            name,
            region,
            description,
        })
    }
}
/// Stores one Effect configuration record.
#[allow(non_camel_case_types)]
#[derive(Debug, Clone)]
pub struct Effect {
    /// Configures id.
    pub id: i32,
    /// Configures name.
    pub name: String,
    /// Configures duration_kind.
    pub duration_kind: crate::config::generated::gas::DurationKind,
    /// Configures duration_ticks.
    pub duration_ticks: Option<i32>,
    /// Configures period_ticks.
    pub period_ticks: Option<i32>,
    /// Configures execute_on_applied.
    pub execute_on_applied: bool,
    /// Configures probability.
    pub probability: f32,
    /// Configures asset_tags.
    pub asset_tags: Vec<String>,
    /// Configures granted_tags.
    pub granted_tags: Vec<String>,
    /// Configures modifier_ids.
    pub modifier_ids: Vec<i32>,
}

impl Decode for Effect {
    fn decode(buf: &mut ByteBuf) -> Result<Self, DecodeError> {
        let id: i32 = Decode::decode(buf).map_err(|error| error.context("Effect.id"))?;
        let name: String = Decode::decode(buf).map_err(|error| error.context("Effect.name"))?;
        let duration_kind: crate::config::generated::gas::DurationKind =
            Decode::decode(buf).map_err(|error| error.context("Effect.duration_kind"))?;
        let duration_ticks: Option<i32> =
            Decode::decode(buf).map_err(|error| error.context("Effect.duration_ticks"))?;
        let period_ticks: Option<i32> =
            Decode::decode(buf).map_err(|error| error.context("Effect.period_ticks"))?;
        let execute_on_applied: bool =
            Decode::decode(buf).map_err(|error| error.context("Effect.execute_on_applied"))?;
        let probability: f32 =
            Decode::decode(buf).map_err(|error| error.context("Effect.probability"))?;
        let asset_tags: Vec<String> =
            Decode::decode(buf).map_err(|error| error.context("Effect.asset_tags"))?;
        let granted_tags: Vec<String> =
            Decode::decode(buf).map_err(|error| error.context("Effect.granted_tags"))?;
        let modifier_ids: Vec<i32> =
            Decode::decode(buf).map_err(|error| error.context("Effect.modifier_ids"))?;
        Ok(Self {
            id,
            name,
            duration_kind,
            duration_ticks,
            period_ticks,
            execute_on_applied,
            probability,
            asset_tags,
            granted_tags,
            modifier_ids,
        })
    }
}
/// Stores one Modifier configuration record.
#[allow(non_camel_case_types)]
#[derive(Debug, Clone)]
pub struct Modifier {
    /// Configures id.
    pub id: i32,
    /// Configures attribute.
    pub attribute: String,
    /// Configures operation.
    pub operation: crate::config::generated::gas::ModifierOperation,
    /// Configures magnitude_kind.
    pub magnitude_kind: crate::config::generated::gas::MagnitudeKind,
    /// Configures base.
    pub base: f32,
    /// Configures per_level.
    pub per_level: f32,
}

impl Decode for Modifier {
    fn decode(buf: &mut ByteBuf) -> Result<Self, DecodeError> {
        let id: i32 = Decode::decode(buf).map_err(|error| error.context("Modifier.id"))?;
        let attribute: String =
            Decode::decode(buf).map_err(|error| error.context("Modifier.attribute"))?;
        let operation: crate::config::generated::gas::ModifierOperation =
            Decode::decode(buf).map_err(|error| error.context("Modifier.operation"))?;
        let magnitude_kind: crate::config::generated::gas::MagnitudeKind =
            Decode::decode(buf).map_err(|error| error.context("Modifier.magnitude_kind"))?;
        let base: f32 = Decode::decode(buf).map_err(|error| error.context("Modifier.base"))?;
        let per_level: f32 =
            Decode::decode(buf).map_err(|error| error.context("Modifier.per_level"))?;
        Ok(Self {
            id,
            attribute,
            operation,
            magnitude_kind,
            base,
            per_level,
        })
    }
}
/// Stores one Tag configuration record.
#[allow(non_camel_case_types)]
#[derive(Debug, Clone)]
pub struct Tag {
    /// Configures name.
    pub name: String,
    /// Configures description.
    pub description: String,
}

impl Decode for Tag {
    fn decode(buf: &mut ByteBuf) -> Result<Self, DecodeError> {
        let name: String = Decode::decode(buf).map_err(|error| error.context("Tag.name"))?;
        let description: String =
            Decode::decode(buf).map_err(|error| error.context("Tag.description"))?;
        Ok(Self { name, description })
    }
}
/// Stores one Targeting configuration record.
#[allow(non_camel_case_types)]
#[derive(Debug, Clone)]
pub struct Targeting {
    /// Configures id.
    pub id: i32,
    /// Configures name.
    pub name: String,
    /// Configures selection.
    pub selection: crate::config::generated::gas::SelectionKind,
    /// Configures radius.
    pub radius: Option<f32>,
    /// Configures half_angle_radians.
    pub half_angle_radians: Option<f32>,
    /// Configures max_distance.
    pub max_distance: Option<f32>,
    /// Configures exclude_source.
    pub exclude_source: bool,
    /// Configures require_attributes.
    pub require_attributes: bool,
    /// Configures required_tags.
    pub required_tags: Vec<String>,
    /// Configures blocked_tags.
    pub blocked_tags: Vec<String>,
    /// Configures sort.
    pub sort: crate::config::generated::gas::SortOrder,
    /// Configures limit.
    pub limit: i32,
}

impl Decode for Targeting {
    fn decode(buf: &mut ByteBuf) -> Result<Self, DecodeError> {
        let id: i32 = Decode::decode(buf).map_err(|error| error.context("Targeting.id"))?;
        let name: String = Decode::decode(buf).map_err(|error| error.context("Targeting.name"))?;
        let selection: crate::config::generated::gas::SelectionKind =
            Decode::decode(buf).map_err(|error| error.context("Targeting.selection"))?;
        let radius: Option<f32> =
            Decode::decode(buf).map_err(|error| error.context("Targeting.radius"))?;
        let half_angle_radians: Option<f32> =
            Decode::decode(buf).map_err(|error| error.context("Targeting.half_angle_radians"))?;
        let max_distance: Option<f32> =
            Decode::decode(buf).map_err(|error| error.context("Targeting.max_distance"))?;
        let exclude_source: bool =
            Decode::decode(buf).map_err(|error| error.context("Targeting.exclude_source"))?;
        let require_attributes: bool =
            Decode::decode(buf).map_err(|error| error.context("Targeting.require_attributes"))?;
        let required_tags: Vec<String> =
            Decode::decode(buf).map_err(|error| error.context("Targeting.required_tags"))?;
        let blocked_tags: Vec<String> =
            Decode::decode(buf).map_err(|error| error.context("Targeting.blocked_tags"))?;
        let sort: crate::config::generated::gas::SortOrder =
            Decode::decode(buf).map_err(|error| error.context("Targeting.sort"))?;
        let limit: i32 = Decode::decode(buf).map_err(|error| error.context("Targeting.limit"))?;
        Ok(Self {
            id,
            name,
            selection,
            radius,
            half_angle_radians,
            max_distance,
            exclude_source,
            require_attributes,
            required_tags,
            blocked_tags,
            sort,
            limit,
        })
    }
}

/// Stores gas.TbTag rows and a checked primary-key index.
#[derive(Debug, Clone)]
pub struct TbTag {
    rows: Vec<Arc<crate::config::generated::gas::Tag>>,
    indices: std::collections::BTreeMap<String, usize>,
}

impl TbTag {
    /// Decodes a complete table file, rejecting malformed bytes or duplicate keys.
    pub fn new(buf: &mut ByteBuf) -> Result<Arc<Self>, LubanError> {
        let count = buf
            .read_size()
            .map_err(|error| error.context("gas.TbTag.count"))?;
        let mut rows = Vec::new();
        rows.try_reserve_exact(count)
            .map_err(|error| LubanError::Table(error.to_string()))?;
        for index in 0..count {
            rows.push(
                crate::config::generated::gas::Tag::decode(buf)
                    .map_err(|error| error.context(format!("gas.TbTag row {index}")))?,
            );
        }
        buf.finish().map_err(|error| error.context("gas.TbTag"))?;
        Ok(Arc::new(Self::from_rows(rows)?))
    }

    /// Builds an indexed table from records, returning an error for duplicate keys.
    pub fn from_rows(rows: Vec<crate::config::generated::gas::Tag>) -> Result<Self, LubanError> {
        let mut indices = std::collections::BTreeMap::new();
        let mut shared_rows = Vec::with_capacity(rows.len());
        for row in rows {
            let key = row.name.clone();
            if indices.contains_key(&key) {
                return Err(LubanError::Table(format!(
                    "duplicate gas.TbTag key: {key:?}"
                )));
            }
            indices.insert(key, shared_rows.len());
            shared_rows.push(Arc::new(row));
        }
        Ok(Self {
            rows: shared_rows,
            indices,
        })
    }

    /// Returns the matching record, or `None` if the primary key is absent.
    pub fn get<Q>(&self, key: &Q) -> Option<&Arc<crate::config::generated::gas::Tag>>
    where
        String: std::borrow::Borrow<Q>,
        Q: Ord + ?Sized,
    {
        self.indices
            .get(key)
            .and_then(|index| self.rows.get(*index))
    }

    /// Iterates through records in exported row order.
    pub fn iter(&self) -> impl ExactSizeIterator<Item = &Arc<crate::config::generated::gas::Tag>> {
        self.rows.iter()
    }

    /// Returns the number of records in this table.
    pub fn len(&self) -> usize {
        self.rows.len()
    }

    /// Returns whether this table has no records.
    pub fn is_empty(&self) -> bool {
        self.rows.is_empty()
    }
}
/// Stores gas.TbAttribute rows and a checked primary-key index.
#[derive(Debug, Clone)]
pub struct TbAttribute {
    rows: Vec<Arc<crate::config::generated::gas::Attribute>>,
    indices: std::collections::BTreeMap<String, usize>,
}

impl TbAttribute {
    /// Decodes a complete table file, rejecting malformed bytes or duplicate keys.
    pub fn new(buf: &mut ByteBuf) -> Result<Arc<Self>, LubanError> {
        let count = buf
            .read_size()
            .map_err(|error| error.context("gas.TbAttribute.count"))?;
        let mut rows = Vec::new();
        rows.try_reserve_exact(count)
            .map_err(|error| LubanError::Table(error.to_string()))?;
        for index in 0..count {
            rows.push(
                crate::config::generated::gas::Attribute::decode(buf)
                    .map_err(|error| error.context(format!("gas.TbAttribute row {index}")))?,
            );
        }
        buf.finish()
            .map_err(|error| error.context("gas.TbAttribute"))?;
        Ok(Arc::new(Self::from_rows(rows)?))
    }

    /// Builds an indexed table from records, returning an error for duplicate keys.
    pub fn from_rows(
        rows: Vec<crate::config::generated::gas::Attribute>,
    ) -> Result<Self, LubanError> {
        let mut indices = std::collections::BTreeMap::new();
        let mut shared_rows = Vec::with_capacity(rows.len());
        for row in rows {
            let key = row.name.clone();
            if indices.contains_key(&key) {
                return Err(LubanError::Table(format!(
                    "duplicate gas.TbAttribute key: {key:?}"
                )));
            }
            indices.insert(key, shared_rows.len());
            shared_rows.push(Arc::new(row));
        }
        Ok(Self {
            rows: shared_rows,
            indices,
        })
    }

    /// Returns the matching record, or `None` if the primary key is absent.
    pub fn get<Q>(&self, key: &Q) -> Option<&Arc<crate::config::generated::gas::Attribute>>
    where
        String: std::borrow::Borrow<Q>,
        Q: Ord + ?Sized,
    {
        self.indices
            .get(key)
            .and_then(|index| self.rows.get(*index))
    }

    /// Iterates through records in exported row order.
    pub fn iter(
        &self,
    ) -> impl ExactSizeIterator<Item = &Arc<crate::config::generated::gas::Attribute>> {
        self.rows.iter()
    }

    /// Returns the number of records in this table.
    pub fn len(&self) -> usize {
        self.rows.len()
    }

    /// Returns whether this table has no records.
    pub fn is_empty(&self) -> bool {
        self.rows.is_empty()
    }
}
/// Stores gas.TbEffect rows and a checked primary-key index.
#[derive(Debug, Clone)]
pub struct TbEffect {
    rows: Vec<Arc<crate::config::generated::gas::Effect>>,
    indices: std::collections::BTreeMap<i32, usize>,
}

impl TbEffect {
    /// Decodes a complete table file, rejecting malformed bytes or duplicate keys.
    pub fn new(buf: &mut ByteBuf) -> Result<Arc<Self>, LubanError> {
        let count = buf
            .read_size()
            .map_err(|error| error.context("gas.TbEffect.count"))?;
        let mut rows = Vec::new();
        rows.try_reserve_exact(count)
            .map_err(|error| LubanError::Table(error.to_string()))?;
        for index in 0..count {
            rows.push(
                crate::config::generated::gas::Effect::decode(buf)
                    .map_err(|error| error.context(format!("gas.TbEffect row {index}")))?,
            );
        }
        buf.finish()
            .map_err(|error| error.context("gas.TbEffect"))?;
        Ok(Arc::new(Self::from_rows(rows)?))
    }

    /// Builds an indexed table from records, returning an error for duplicate keys.
    pub fn from_rows(rows: Vec<crate::config::generated::gas::Effect>) -> Result<Self, LubanError> {
        let mut indices = std::collections::BTreeMap::new();
        let mut shared_rows = Vec::with_capacity(rows.len());
        for row in rows {
            let key = row.id;
            if indices.contains_key(&key) {
                return Err(LubanError::Table(format!(
                    "duplicate gas.TbEffect key: {key:?}"
                )));
            }
            indices.insert(key, shared_rows.len());
            shared_rows.push(Arc::new(row));
        }
        Ok(Self {
            rows: shared_rows,
            indices,
        })
    }

    /// Returns the matching record, or `None` if the primary key is absent.
    pub fn get<Q>(&self, key: &Q) -> Option<&Arc<crate::config::generated::gas::Effect>>
    where
        i32: std::borrow::Borrow<Q>,
        Q: Ord + ?Sized,
    {
        self.indices
            .get(key)
            .and_then(|index| self.rows.get(*index))
    }

    /// Iterates through records in exported row order.
    pub fn iter(
        &self,
    ) -> impl ExactSizeIterator<Item = &Arc<crate::config::generated::gas::Effect>> {
        self.rows.iter()
    }

    /// Returns the number of records in this table.
    pub fn len(&self) -> usize {
        self.rows.len()
    }

    /// Returns whether this table has no records.
    pub fn is_empty(&self) -> bool {
        self.rows.is_empty()
    }
}
/// Stores gas.TbModifier rows and a checked primary-key index.
#[derive(Debug, Clone)]
pub struct TbModifier {
    rows: Vec<Arc<crate::config::generated::gas::Modifier>>,
    indices: std::collections::BTreeMap<i32, usize>,
}

impl TbModifier {
    /// Decodes a complete table file, rejecting malformed bytes or duplicate keys.
    pub fn new(buf: &mut ByteBuf) -> Result<Arc<Self>, LubanError> {
        let count = buf
            .read_size()
            .map_err(|error| error.context("gas.TbModifier.count"))?;
        let mut rows = Vec::new();
        rows.try_reserve_exact(count)
            .map_err(|error| LubanError::Table(error.to_string()))?;
        for index in 0..count {
            rows.push(
                crate::config::generated::gas::Modifier::decode(buf)
                    .map_err(|error| error.context(format!("gas.TbModifier row {index}")))?,
            );
        }
        buf.finish()
            .map_err(|error| error.context("gas.TbModifier"))?;
        Ok(Arc::new(Self::from_rows(rows)?))
    }

    /// Builds an indexed table from records, returning an error for duplicate keys.
    pub fn from_rows(
        rows: Vec<crate::config::generated::gas::Modifier>,
    ) -> Result<Self, LubanError> {
        let mut indices = std::collections::BTreeMap::new();
        let mut shared_rows = Vec::with_capacity(rows.len());
        for row in rows {
            let key = row.id;
            if indices.contains_key(&key) {
                return Err(LubanError::Table(format!(
                    "duplicate gas.TbModifier key: {key:?}"
                )));
            }
            indices.insert(key, shared_rows.len());
            shared_rows.push(Arc::new(row));
        }
        Ok(Self {
            rows: shared_rows,
            indices,
        })
    }

    /// Returns the matching record, or `None` if the primary key is absent.
    pub fn get<Q>(&self, key: &Q) -> Option<&Arc<crate::config::generated::gas::Modifier>>
    where
        i32: std::borrow::Borrow<Q>,
        Q: Ord + ?Sized,
    {
        self.indices
            .get(key)
            .and_then(|index| self.rows.get(*index))
    }

    /// Iterates through records in exported row order.
    pub fn iter(
        &self,
    ) -> impl ExactSizeIterator<Item = &Arc<crate::config::generated::gas::Modifier>> {
        self.rows.iter()
    }

    /// Returns the number of records in this table.
    pub fn len(&self) -> usize {
        self.rows.len()
    }

    /// Returns whether this table has no records.
    pub fn is_empty(&self) -> bool {
        self.rows.is_empty()
    }
}
/// Stores gas.TbAbility rows and a checked primary-key index.
#[derive(Debug, Clone)]
pub struct TbAbility {
    rows: Vec<Arc<crate::config::generated::gas::Ability>>,
    indices: std::collections::BTreeMap<i32, usize>,
}

impl TbAbility {
    /// Decodes a complete table file, rejecting malformed bytes or duplicate keys.
    pub fn new(buf: &mut ByteBuf) -> Result<Arc<Self>, LubanError> {
        let count = buf
            .read_size()
            .map_err(|error| error.context("gas.TbAbility.count"))?;
        let mut rows = Vec::new();
        rows.try_reserve_exact(count)
            .map_err(|error| LubanError::Table(error.to_string()))?;
        for index in 0..count {
            rows.push(
                crate::config::generated::gas::Ability::decode(buf)
                    .map_err(|error| error.context(format!("gas.TbAbility row {index}")))?,
            );
        }
        buf.finish()
            .map_err(|error| error.context("gas.TbAbility"))?;
        Ok(Arc::new(Self::from_rows(rows)?))
    }

    /// Builds an indexed table from records, returning an error for duplicate keys.
    pub fn from_rows(
        rows: Vec<crate::config::generated::gas::Ability>,
    ) -> Result<Self, LubanError> {
        let mut indices = std::collections::BTreeMap::new();
        let mut shared_rows = Vec::with_capacity(rows.len());
        for row in rows {
            let key = row.id;
            if indices.contains_key(&key) {
                return Err(LubanError::Table(format!(
                    "duplicate gas.TbAbility key: {key:?}"
                )));
            }
            indices.insert(key, shared_rows.len());
            shared_rows.push(Arc::new(row));
        }
        Ok(Self {
            rows: shared_rows,
            indices,
        })
    }

    /// Returns the matching record, or `None` if the primary key is absent.
    pub fn get<Q>(&self, key: &Q) -> Option<&Arc<crate::config::generated::gas::Ability>>
    where
        i32: std::borrow::Borrow<Q>,
        Q: Ord + ?Sized,
    {
        self.indices
            .get(key)
            .and_then(|index| self.rows.get(*index))
    }

    /// Iterates through records in exported row order.
    pub fn iter(
        &self,
    ) -> impl ExactSizeIterator<Item = &Arc<crate::config::generated::gas::Ability>> {
        self.rows.iter()
    }

    /// Returns the number of records in this table.
    pub fn len(&self) -> usize {
        self.rows.len()
    }

    /// Returns whether this table has no records.
    pub fn is_empty(&self) -> bool {
        self.rows.is_empty()
    }
}
/// Stores gas.TbAbilityAdditionalCost rows and a checked primary-key index.
#[derive(Debug, Clone)]
pub struct TbAbilityAdditionalCost {
    rows: Vec<Arc<crate::config::generated::gas::AbilityAdditionalCost>>,
    indices: std::collections::BTreeMap<i32, usize>,
}

impl TbAbilityAdditionalCost {
    /// Decodes a complete table file, rejecting malformed bytes or duplicate keys.
    pub fn new(buf: &mut ByteBuf) -> Result<Arc<Self>, LubanError> {
        let count = buf
            .read_size()
            .map_err(|error| error.context("gas.TbAbilityAdditionalCost.count"))?;
        let mut rows = Vec::new();
        rows.try_reserve_exact(count)
            .map_err(|error| LubanError::Table(error.to_string()))?;
        for index in 0..count {
            rows.push(
                crate::config::generated::gas::AbilityAdditionalCost::decode(buf).map_err(
                    |error| error.context(format!("gas.TbAbilityAdditionalCost row {index}")),
                )?,
            );
        }
        buf.finish()
            .map_err(|error| error.context("gas.TbAbilityAdditionalCost"))?;
        Ok(Arc::new(Self::from_rows(rows)?))
    }

    /// Builds an indexed table from records, returning an error for duplicate keys.
    pub fn from_rows(
        rows: Vec<crate::config::generated::gas::AbilityAdditionalCost>,
    ) -> Result<Self, LubanError> {
        let mut indices = std::collections::BTreeMap::new();
        let mut shared_rows = Vec::with_capacity(rows.len());
        for row in rows {
            let key = row.id;
            if indices.contains_key(&key) {
                return Err(LubanError::Table(format!(
                    "duplicate gas.TbAbilityAdditionalCost key: {key:?}"
                )));
            }
            indices.insert(key, shared_rows.len());
            shared_rows.push(Arc::new(row));
        }
        Ok(Self {
            rows: shared_rows,
            indices,
        })
    }

    /// Returns the matching record, or `None` if the primary key is absent.
    pub fn get<Q>(
        &self,
        key: &Q,
    ) -> Option<&Arc<crate::config::generated::gas::AbilityAdditionalCost>>
    where
        i32: std::borrow::Borrow<Q>,
        Q: Ord + ?Sized,
    {
        self.indices
            .get(key)
            .and_then(|index| self.rows.get(*index))
    }

    /// Iterates through records in exported row order.
    pub fn iter(
        &self,
    ) -> impl ExactSizeIterator<Item = &Arc<crate::config::generated::gas::AbilityAdditionalCost>>
    {
        self.rows.iter()
    }

    /// Returns the number of records in this table.
    pub fn len(&self) -> usize {
        self.rows.len()
    }

    /// Returns whether this table has no records.
    pub fn is_empty(&self) -> bool {
        self.rows.is_empty()
    }
}
/// Stores gas.TbAbilityTask rows and a checked primary-key index.
#[derive(Debug, Clone)]
pub struct TbAbilityTask {
    rows: Vec<Arc<crate::config::generated::gas::AbilityTask>>,
    indices: std::collections::BTreeMap<i32, usize>,
}

impl TbAbilityTask {
    /// Decodes a complete table file, rejecting malformed bytes or duplicate keys.
    pub fn new(buf: &mut ByteBuf) -> Result<Arc<Self>, LubanError> {
        let count = buf
            .read_size()
            .map_err(|error| error.context("gas.TbAbilityTask.count"))?;
        let mut rows = Vec::new();
        rows.try_reserve_exact(count)
            .map_err(|error| LubanError::Table(error.to_string()))?;
        for index in 0..count {
            rows.push(
                crate::config::generated::gas::AbilityTask::decode(buf)
                    .map_err(|error| error.context(format!("gas.TbAbilityTask row {index}")))?,
            );
        }
        buf.finish()
            .map_err(|error| error.context("gas.TbAbilityTask"))?;
        Ok(Arc::new(Self::from_rows(rows)?))
    }

    /// Builds an indexed table from records, returning an error for duplicate keys.
    pub fn from_rows(
        rows: Vec<crate::config::generated::gas::AbilityTask>,
    ) -> Result<Self, LubanError> {
        let mut indices = std::collections::BTreeMap::new();
        let mut shared_rows = Vec::with_capacity(rows.len());
        for row in rows {
            let key = row.id;
            if indices.contains_key(&key) {
                return Err(LubanError::Table(format!(
                    "duplicate gas.TbAbilityTask key: {key:?}"
                )));
            }
            indices.insert(key, shared_rows.len());
            shared_rows.push(Arc::new(row));
        }
        Ok(Self {
            rows: shared_rows,
            indices,
        })
    }

    /// Returns the matching record, or `None` if the primary key is absent.
    pub fn get<Q>(&self, key: &Q) -> Option<&Arc<crate::config::generated::gas::AbilityTask>>
    where
        i32: std::borrow::Borrow<Q>,
        Q: Ord + ?Sized,
    {
        self.indices
            .get(key)
            .and_then(|index| self.rows.get(*index))
    }

    /// Iterates through records in exported row order.
    pub fn iter(
        &self,
    ) -> impl ExactSizeIterator<Item = &Arc<crate::config::generated::gas::AbilityTask>> {
        self.rows.iter()
    }

    /// Returns the number of records in this table.
    pub fn len(&self) -> usize {
        self.rows.len()
    }

    /// Returns whether this table has no records.
    pub fn is_empty(&self) -> bool {
        self.rows.is_empty()
    }
}
/// Stores gas.TbTargeting rows and a checked primary-key index.
#[derive(Debug, Clone)]
pub struct TbTargeting {
    rows: Vec<Arc<crate::config::generated::gas::Targeting>>,
    indices: std::collections::BTreeMap<i32, usize>,
}

impl TbTargeting {
    /// Decodes a complete table file, rejecting malformed bytes or duplicate keys.
    pub fn new(buf: &mut ByteBuf) -> Result<Arc<Self>, LubanError> {
        let count = buf
            .read_size()
            .map_err(|error| error.context("gas.TbTargeting.count"))?;
        let mut rows = Vec::new();
        rows.try_reserve_exact(count)
            .map_err(|error| LubanError::Table(error.to_string()))?;
        for index in 0..count {
            rows.push(
                crate::config::generated::gas::Targeting::decode(buf)
                    .map_err(|error| error.context(format!("gas.TbTargeting row {index}")))?,
            );
        }
        buf.finish()
            .map_err(|error| error.context("gas.TbTargeting"))?;
        Ok(Arc::new(Self::from_rows(rows)?))
    }

    /// Builds an indexed table from records, returning an error for duplicate keys.
    pub fn from_rows(
        rows: Vec<crate::config::generated::gas::Targeting>,
    ) -> Result<Self, LubanError> {
        let mut indices = std::collections::BTreeMap::new();
        let mut shared_rows = Vec::with_capacity(rows.len());
        for row in rows {
            let key = row.id;
            if indices.contains_key(&key) {
                return Err(LubanError::Table(format!(
                    "duplicate gas.TbTargeting key: {key:?}"
                )));
            }
            indices.insert(key, shared_rows.len());
            shared_rows.push(Arc::new(row));
        }
        Ok(Self {
            rows: shared_rows,
            indices,
        })
    }

    /// Returns the matching record, or `None` if the primary key is absent.
    pub fn get<Q>(&self, key: &Q) -> Option<&Arc<crate::config::generated::gas::Targeting>>
    where
        i32: std::borrow::Borrow<Q>,
        Q: Ord + ?Sized,
    {
        self.indices
            .get(key)
            .and_then(|index| self.rows.get(*index))
    }

    /// Iterates through records in exported row order.
    pub fn iter(
        &self,
    ) -> impl ExactSizeIterator<Item = &Arc<crate::config::generated::gas::Targeting>> {
        self.rows.iter()
    }

    /// Returns the number of records in this table.
    pub fn len(&self) -> usize {
        self.rows.len()
    }

    /// Returns whether this table has no records.
    pub fn is_empty(&self) -> bool {
        self.rows.is_empty()
    }
}

/// Describes this module's binary field order and enum discriminants.
pub const SCHEMA_DESCRIPTOR: &str = concat!(
    "module:gas;",
    "enum:ActionKind;",
    "ApplyEffect=0;",
    "EndAbility=1;",
    "enum:AttributeRegion;",
    "Hot=0;",
    "Cold=1;",
    "enum:DurationKind;",
    "Instant=0;",
    "DurationTicks=1;",
    "Infinite=2;",
    "enum:MagnitudeKind;",
    "Flat=0;",
    "LinearLevel=1;",
    "enum:ModifierOperation;",
    "Add=0;",
    "PercentAdd=1;",
    "Multiply=2;",
    "Override=3;",
    "enum:SelectionKind;",
    "SelfTarget=0;",
    "ExplicitEntity=1;",
    "Sphere=2;",
    "Cone=3;",
    "enum:SortOrder;",
    "None=0;",
    "Nearest=1;",
    "Farthest=2;",
    "enum:TargetScope;",
    "None=0;",
    "Primary=1;",
    "AllCaptured=2;",
    "bean:Ability;",
    "id:i32;",
    "name:String;",
    "max_level:i32;",
    "cost_effect_id:Option<i32>;",
    "additional_cost_ids:Vec<i32>;",
    "cooldown_effect_id:Option<i32>;",
    "asset_tags:Vec<String>;",
    "required_tags:Vec<String>;",
    "blocked_tags:Vec<String>;",
    "cancel_ability_tags:Vec<String>;",
    "block_ability_tags:Vec<String>;",
    "targeting_id:i32;",
    "allow_multiple_instances:bool;",
    "task_ids:Vec<i32>;",
    "bean:AbilityAdditionalCost;",
    "id:i32;",
    "resource:String;",
    "amount:i64;",
    "bean:AbilityTask;",
    "id:i32;",
    "at_tick:i32;",
    "kind:crate::gas::ActionKind;",
    "target_scope:crate::gas::TargetScope;",
    "effect_id:Option<i32>;",
    "bean:Attribute;",
    "name:String;",
    "region:crate::gas::AttributeRegion;",
    "description:String;",
    "bean:Effect;",
    "id:i32;",
    "name:String;",
    "duration_kind:crate::gas::DurationKind;",
    "duration_ticks:Option<i32>;",
    "period_ticks:Option<i32>;",
    "execute_on_applied:bool;",
    "probability:f32;",
    "asset_tags:Vec<String>;",
    "granted_tags:Vec<String>;",
    "modifier_ids:Vec<i32>;",
    "bean:Modifier;",
    "id:i32;",
    "attribute:String;",
    "operation:crate::gas::ModifierOperation;",
    "magnitude_kind:crate::gas::MagnitudeKind;",
    "base:f32;",
    "per_level:f32;",
    "bean:Tag;",
    "name:String;",
    "description:String;",
    "bean:Targeting;",
    "id:i32;",
    "name:String;",
    "selection:crate::gas::SelectionKind;",
    "radius:Option<f32>;",
    "half_angle_radians:Option<f32>;",
    "max_distance:Option<f32>;",
    "exclude_source:bool;",
    "require_attributes:bool;",
    "required_tags:Vec<String>;",
    "blocked_tags:Vec<String>;",
    "sort:crate::gas::SortOrder;",
    "limit:i32;",
    "table:gas.TbTag:crate::gas::Tag:name:gas_tbtag;",
    "table:gas.TbAttribute:crate::gas::Attribute:name:gas_tbattribute;",
    "table:gas.TbEffect:crate::gas::Effect:id:gas_tbeffect;",
    "table:gas.TbModifier:crate::gas::Modifier:id:gas_tbmodifier;",
    "table:gas.TbAbility:crate::gas::Ability:id:gas_tbability;",
    "table:gas.TbAbilityAdditionalCost:crate::gas::AbilityAdditionalCost:id:gas_tbabilityadditionalcost;",
    "table:gas.TbAbilityTask:crate::gas::AbilityTask:id:gas_tbabilitytask;",
    "table:gas.TbTargeting:crate::gas::Targeting:id:gas_tbtargeting;",
);
